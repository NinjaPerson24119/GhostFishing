using Godot;

public partial class InventoryUI : Control {
    /*
    private Inventory _playerInventory;

    public override void _Ready() {
        _playerInventory = PersistentState.Ref().GetPlayerState().PlayerInventory;
        for (int x = 0; x < _playerInventory.Width; ++x) {
            for (int y = 0; y < _playerInventory.Height; ++y) {

            }
        }
    }
    */
}
